<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper>
    <select id="catList" resultType="list" parameterType="string">
        select cname from cats where cwhichboard=#{cwhichboard}
    </select>
</mapper>